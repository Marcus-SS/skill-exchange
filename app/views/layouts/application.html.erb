<!DOCTYPE html>
<html>
  <head>
    <title>SkillExchange</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div data-controller="notification" data-notification-user-id-value="<%= current_user.id if user_signed_in? %>">
      <%= render "shared/navbar" unless action_name == 'new' && controller_name == 'sessions' %>
      <%= render "shared/flashes" %>

    <%= yield %>
  </body>
</html>
